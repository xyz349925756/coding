#!/usr/bin/python
x = 2
if x == 1:
    print("X =",x)
else:
    print("X =",x)
x = x+1
print("X =",x)

#这里重点是x=x+1 这里缩进的层次不同运行所得到的结果也不同。
#x=1 符合 x == 1 所以输出：x = 1
#定义x=2 不符合x == 1的条件 所以执行 else （第一层）下面的关系层print("X=",x)
#输出x=2.
#接着执行第一层关系x=x+1
#这里是上面判断的结果+1
#接着是输出x=x+1的结果。
